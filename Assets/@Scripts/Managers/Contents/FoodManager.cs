using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FoodManager : MonoBehaviour
{
    /*
    public SpriteRenderer[] DisplaySprite;

    private Queue<FoodData> _foodQueue = new Queue<FoodData>();




    public void Enqueue(FoodData food)
    {
        _foodQueue.Enqueue(food);
    }

    public FoodData Dequeue()
    {
        if (_foodQueue.Count == 0)
        {
            Debug.Log("Empty!");
            return null;
        }
        return _foodQueue.Dequeue();
    }

    
    public void SetSprite()
    {
        int index = 0;
        foreach(var food in _foodQueue)
        {
            if(index >= DisplaySprite.Length)
                break;

            DisplaySprite[index].sprite = food.sprite;
            index++;
        }
    }
    public void Clear()
    {
        _foodQueue.Clear();
    }
    */

}
